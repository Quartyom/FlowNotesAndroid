package quartyom.flownotes.core;

// Note data model
// All fields are public, because it's all ruled via Focus
public class Note {
    public String initialText;
    public String savedText;
    public String currentText;
}
