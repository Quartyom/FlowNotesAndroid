package quartyom.flownotes.android.focus;

import quartyom.flownotes.core.Focus;

// not a real ViewModel because it should never be recreated
public class FocusViewModel {
    protected static Focus focus;
}
